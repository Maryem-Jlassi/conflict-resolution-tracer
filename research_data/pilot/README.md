# Pilot workspace

This directory is intentionally empty of real cases and labels. Use `pilot_cli` to hash human-selected official source material and create blank episode shells. Selection must not inspect LCM or baseline predictions. Annotation packages and their private randomization mappings must be stored separately; mappings must never be distributed to annotators.
